import React from "react";

import { HashRouter, Route } from "react-router-dom";

export default function Logo() {
    return (
        <div id="logo">
            <img src="/logo.png" />
        </div>
    );
}
